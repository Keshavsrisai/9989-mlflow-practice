# 9989-mlflow-practice 
## PRACTICE REPO (MLOPS)

### PolynomialRegression to compare between runs

Degree from 1 to 10 

### RandomForestRegressor with GridsearchCV for hyperparameter tuning

This is a practice repo for Mlflow tracking - best model is logged

### ANN with hyperopt for hyperparameter tuning

A simple ANN with single hidden layer with same dataset is used with parameter tuning applied on learning rate and momentum

### TO-DO - DVC, DAGSHUB, DOCKER image creation and deployment to AWS
